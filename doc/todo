. read_state ne retourne rien, mais alarm -> hopper_check + ready

. chez miguel, il me semble que si erreur lors de lecture,
  je dump quand meme une carte. donc bitmap. donc alarms mal
  lues?

. tester
 . test du reset
 . le bit M600_ERROR n a jamais ete dumpe, oublie lors des
 tests precedent. dans le doute, on le SET comme les autre
 ssi il est a 0 volts, mais il faut tester ca via un get_state
 . test des donnees lues: endianness ect...
